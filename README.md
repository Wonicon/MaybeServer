# A HTTP Server(?) that only supports GET...

Used to learn socket programming...

Now the server has two problems:

1. Requests from an html page more than 6 may cause **pending** in chrome;
2. Some files like favicon may not end the downloading process.

